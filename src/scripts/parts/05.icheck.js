/**
 * ICheck - in subscribe block
 */
(function($) {
	$('.js-checkbox').iCheck({
		checkboxClass: "checkbox-styled",
	});
	$('.js-checkbox-small').iCheck({
		checkboxClass: "checkbox-styled checkbox-styled_small",
	});
})(jQuery);